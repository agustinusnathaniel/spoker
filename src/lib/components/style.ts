import type { GridProps } from '@chakra-ui/react';

export const contraGridStyle: Partial<GridProps> = {
  borderColor: 'black',
  borderRadius: 16,
  borderWidth: '2px',
  boxShadow: '0px 6px 0px black',
  padding: 4,
};
